{
  "name": "kontoret-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && node scripts/balance.ts && vite build",
    "test": "playwright test",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource/pixelify-sans": "^5.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
